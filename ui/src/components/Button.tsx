// Re-export of the canonical Button primitive under its original import path.
// The implementation lives in `ui/button.tsx` alongside the other shadcn-style
// primitives (Input, Checkbox, Tabs, ...).
export { Button, buttonVariants, type ButtonProps } from './ui/button';
